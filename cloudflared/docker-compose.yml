services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: ai-edu-cloudflared
    restart: unless-stopped
    network_mode: host
    command: tunnel --no-autoupdate run --token ${MOVAN_TUNNEL_TOKEN}